CREATE TABLE TAGS (
     ID NUMBER(19) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
     USER_ID NUMBER(19) NOT NULL,
     LABEL VARCHAR2(255 CHAR) NOT NULL,
     COLOR VARCHAR2(50 CHAR) NOT NULL,
     VISIBILITY VARCHAR2(255 CHAR) DEFAULT 'PRIVATE' NOT NULL,
     POSITION NUMBER(19) NOT NULL,
     CREATED_AT TIMESTAMP NOT NULL,
     UPDATED_AT TIMESTAMP NOT NULL,
     CONSTRAINT fk_tags_user FOREIGN KEY (USER_ID) REFERENCES USERS(ID) ON DELETE CASCADE
  );
SELECT DBMS_METADATA.GET_DDL('TABLE', 'TAGS', 'ILJI') FROM DUAL;
SELECT DBMS_METADATA.GET_DDL('TABLE', 'SCHEDULES', 'ILJI') FROM DUAL;

SELECT * from tags;
UPDATE tags
SET visibility = 'PUBLIC'
WHERE id = 65;

-- 66번 태그를 'MUTUAL_FRIENDS'로 변경
UPDATE tags
SET visibility = 'MUTUAL_FRIENDS'
    WHERE id = 66;

-- 변경사항 최종 저장
COMMIT;

select * from TAGS;
